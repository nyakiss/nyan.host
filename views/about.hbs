{{#each objects}}
	<ul class="rate">
		<h2>{{title}}</h2>
		{{#each items}}
			<li>
				<span class="cpu">{{cpu}}</span>
				<span class="ram">{{ram}}</span>
        <span class="rom">{{rom}}</span>
				{{#if spd}}<span class="spd">{{spd}}</span>{{/if}}
			</li>
		{{/each}}
	</ul>
{{/each}}
